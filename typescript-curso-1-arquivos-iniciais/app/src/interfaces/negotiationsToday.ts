//just to ensure that the backend's return will be parsed to the correct properties
export interface NegotiationsToday {

    //thi interface has 2 properties from the external API
    montante: number;
    vezes: number;
}